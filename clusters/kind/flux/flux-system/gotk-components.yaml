# This file contains the Flux components
# In a real deployment, this would be generated by `flux install`
# For testing purposes, this is a placeholder that references the base Flux installation

apiVersion: v1
kind: Namespace
metadata:
  name: flux-system
---
# Note: In actual bootstrap, this file would contain the full Flux component manifests
# generated by `flux install`. For testing, we keep it minimal since the bootstrap
# process handles the actual Flux installation.
